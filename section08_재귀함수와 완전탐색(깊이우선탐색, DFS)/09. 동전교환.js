function solution(m, arr) {
    let answer = Number.MAX_SAFE_INTEGER;
    function DFS(L, sum) {

    }
    DFS(0, 0);
    return answer;
}

let arr = [1, 2, 5];
console.log(solution(15, arr));