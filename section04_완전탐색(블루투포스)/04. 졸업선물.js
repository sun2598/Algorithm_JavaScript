// 선생님은 올해 졸업하는 반 학생들에게 졸업선물을 주려고 합니다.
// 학생들에게 인터넷 쇼핑몰에서 각자 원하는 상품을 골라 그 상품의 가격과 배송비를 제출하라고 했습니다. 
// 선생님이 가지고 있는 예산은 한정되어 있습니다.
// 현재 예산으로 최대 몇 명의 학생에게 선물을 사줄 수 있는지 구하는 프로그램을 작성하세요. 
// 선생님은 상품 하나를 50% 할인해서(반 가격) 살 수 있는 쿠폰을 가지고 있습니다. 배송비는 할인에 포함되지 않습니다.

function solution(m, product) {
    let answer = 0; // 최대 몇 명의 학생에게 선물을 줄 수 있는지 저장할 변수

    let n = product.length; // 제출된 상품의 총 개수
    // 상품의 가격과 배송비의 합을 기준으로 오름차순 정렬
    product.sort(function(a, b) {
        return (a[0] + a[1]) - (b[0] + b[1]);
    });

    // 할인 쿠폰을 사용할 상품을 선택
    for (let i = 0; i < n; i++) {
        // 선택된 상품에 할인 쿠폰을 적용하고 남은 예산 계산
        let money = m - (product[i][0] / 2 + product[i][1]);
        let cnt = 1; // 할인 쿠폰을 사용한 상품은 이미 구매했으므로 카운트 1부터 시작

        // 남은 상품에 대해 예산 내에서 구매할 수 있는지 확인
        for (let j = 0; j < n; j++) {
            // 할인 쿠폰을 이미 사용한 상품은 제외
            if (j !== i && (product[j][0] + product[j][1]) > money) {
                break; // 예산을 초과하면 더 이상 구매 불가능
            }
            if (j !== i && (product[j][0] + product[j][1]) <= money) {
                money -= (product[j][0] + product[j][1]); // 예산에서 상품 가격 차감
                cnt++; // 선물 가능한 학생 수 증가
            }
        }
        answer = Math.max(answer, cnt); // 최대 선물 가능한 학생 수 업데이트
    }

    return answer; // 최종적으로 선물을 줄 수 있는 학생의 최대 수 반환
}


let arr = [
    [6, 6],
    [2, 2],
    [4, 3],
    [4, 5],
    [10, 3],
];
console.log(solution(28, arr));